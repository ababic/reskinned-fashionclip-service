# Optional: speeds up Docker model bake (build-time only; not used at Lambda runtime).
# HF_TOKEN=

# Optional: local smoke tests against staging/production.
# PRINT_VISION_URL=
# PRINT_VISION_API_KEY=

# Optional: local scoring with live HF download (omit FASHIONCLIP_MODEL_DIR).
# SENTRY_DSN=
